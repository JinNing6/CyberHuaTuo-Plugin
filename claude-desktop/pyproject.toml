[project]
name = "cyberhuatuo-claude-desktop"
version = "0.2.1"
description = "Claude Desktop MCPB wrapper for CyberHuaTuo."
requires-python = ">=3.10"
dependencies = [
    "cyberhuatuo==0.2.1",
]

[tool.uv]
package = false
